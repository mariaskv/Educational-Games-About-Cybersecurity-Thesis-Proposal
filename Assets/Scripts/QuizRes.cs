using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuizRes : MonoBehaviour
{

    public GameObject pn;

    // Start is called before the first frame update
    void Start()
    {
        pn.SetActive(false);    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
